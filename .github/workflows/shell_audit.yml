name: Auditoría y Auto-Formato Shell

on: [push, pull_request]

permissions:
  contents: write

jobs:
  shell-quality:
    name: Estilo (Auto) y Seguridad (Manual)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Instalar ShellCheck y Shfmt
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck shfmt

      # --- PASO 1: ESTILO (Auto-Commit) ---
      - name: Aplicar shfmt y Auto-Commit
        id: format_step
        run: |
          find . -type f -name "*.sh" -exec shfmt -w {} +
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"

          if ! git diff --exit-code; then
            git add .
            git commit -m "style(sh): auto-formato aplicado con shfmt"
            git push
            echo "estilo_cambiado=true" >> $GITHUB_OUTPUT
          else
            echo "estilo_cambiado=false" >> $GITHUB_OUTPUT
          fi

      # --- PASO 2: SEGURIDAD (LOGS Y RECOLECCIÓN) ---
      - name: Auditoría de Seguridad con ShellCheck
        id: audit_step
        run: |
          mkdir -p parches_seguridad
          HUBO_ERRORES=false
          HUBO_PARCHES=false
          LISTA_SIN_PARCHE=""

          while IFS= read -r archivo; do
            # Ejecutar shellcheck
            if ! shellcheck "$archivo"; then
              HUBO_ERRORES=true
              
              # Intentar generar parche
              nombre_archivo=$(basename "$archivo")
              ruta_parche="parches_seguridad/${nombre_archivo}.patch"
              
              if shellcheck -f diff "$archivo" > "$ruta_parche" 2>/dev/null && [ -s "$ruta_parche" ]; then
                HUBO_PARCHES=true
              else
                rm -f "$ruta_parche"
                # Si hay error pero no hay parche, lo guardamos para el reporte final
                LISTA_SIN_PARCHE="$LISTA_SIN_PARCHE $archivo"
              fi
            fi
          done < <(find . -type f -name "*.sh")

          # Exportar variables para el paso final
          echo "hubo_errores=$HUBO_ERRORES" >> $GITHUB_OUTPUT
          echo "hubo_parches=$HUBO_PARCHES" >> $GITHUB_OUTPUT
          echo "lista_sin_parche=$LISTA_SIN_PARCHE" >> $GITHUB_OUTPUT

      # --- PASO 3: SUBIDA DE ARTEFACTOS ---
      - name: Subir Parches
        if: steps.audit_step.outputs.hubo_parches == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: parches-de-seguridad
          path: parches_seguridad/*.patch

      # --- PASO 4: REPORTE FINAL DETALLADO ---
      - name: Verificar Resultado Final
        run: |
          echo "=========================================================="
          
          # Reporte de Estilo
          if [ "${{ steps.format_step.outputs.estilo_cambiado }}" == "true" ]; then
            echo "✅ El estilo se arregló automáticamente."
          else
            echo "✅ El estilo ya era correcto, no hubo cambios."
          fi

          # Reporte de Seguridad General
          if [ "${{ steps.audit_step.outputs.hubo_errores }}" == "true" ]; then
            echo "❌ ShellCheck encontró problemas de seguridad/lógica."
            
            # Aviso de Parches
            if [ "${{ steps.audit_step.outputs.hubo_parches }}" == "true" ]; then
              echo "📩 Descarga los parches en la pestaña 'Actions' para corregirlos automáticamente."
            else
              echo "⚠️ No se crearon parches automáticos (los errores requieren cambios manuales)."
            fi

            # Listado de archivos con errores sin parche y sus logs
            ARCHIVOS_MANUALES="${{ steps.audit_step.outputs.lista_sin_parche }}"
            if [ ! -z "$ARCHIVOS_MANUALES" ]; then
              echo ""
              echo "Archivos que no se crearon parches pero que si se detectaron errores:"
              for archivo in $ARCHIVOS_MANUALES; do
                echo "----------------------------------------------------------"
                echo "📄 ARCHIVO: $archivo"
                shellcheck "$archivo" || true
              done
            fi
            
            echo "=========================================================="
            exit 1
          else
            echo "✅ ShellCheck no encontró errores de seguridad."
            echo "=========================================================="
          fi
